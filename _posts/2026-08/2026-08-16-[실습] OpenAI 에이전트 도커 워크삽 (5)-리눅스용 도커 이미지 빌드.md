---
title: "[실습] OpenAI 에이전트 도커 워크삽 (5)-리눅스용 도커 이미지 빌드"
date: 2026-08-16
tags: [오픈AI, OpenAI, GPT-5.6, agenticai, aiagent, openrouter, docker, powershell, fastapi, swagger-ui, visual studio code, Linux, Windows11, Kubernetes, On-Premises ]
typora-root-url: ../
toc: true
categories: [openai]
---

이제 본격적으로 도커로 배포하기 위해 Dockerfile 작성하고 리눅스용 이미지 빌드하는 실습을 해보겠다. 

# 1. 실습 7 - Dockerfile 작성하기

Dockerfile의 Base Image:

```dockerfile
FROM python:3.12-slim
```

이 한 줄이 매우 중요하다.

Windows PC에서 `docker build`를 실행하더라도 이 프로젝트의 최종 application image는 Linux 기반이다.

전체 흐름:

```text
Windows Source Code
       ↓
Dockerfile
       ↓
python:3.12-slim
       ↓
Linux filesystem + Python + app
       ↓
Linux container image
```

API Key는 Dockerfile에 들어 있지 않다.

---
